apiVersion: v1
kind: Secret
metadata:
  name: nginx-secret
data:
  username: "YWRtaW4K"
  password: "YWRtaW4zMjEK"

# secret is used to store sensitive information, such as passwords, OAuth tokens, and SSH keys.
# Secrets are similar to ConfigMaps but are specifically designed to hold sensitive data.